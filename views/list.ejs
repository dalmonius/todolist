<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>to do list ejs</title>
</head>
<body>

    <% if (kindOfDay === 6 || kindOfDay === 0){ %>
        <h1 style="color: purple"><%= kindOfDay %>'s to-do list</h1>
    <% } else { %>
        <h1 style="color: gray"><%= kindOfDay %>'s to-do list</h1>
    <% } %>
</body>
</html>